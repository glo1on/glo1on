// enum to use for character direction

// Silas

public enum Direction {
    NORTH,
    SOUTH,
    EAST,
    WEST;

    // Gave DEADEND -1 value, so it can be associated with staying in the same room.
    public static final int DEADEND = -1;
}
